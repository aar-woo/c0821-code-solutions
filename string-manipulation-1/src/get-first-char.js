/* exported getFirstChar */
/* Pseudo Code
  - create storage for output
  - add character at the first position of the string to output
  - return output
*/

function getFirstChar(string) {
  var firstChar = '';
  firstChar += string[0];
  return firstChar;
}
